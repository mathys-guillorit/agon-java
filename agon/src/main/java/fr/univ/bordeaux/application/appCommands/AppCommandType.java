package fr.univ.bordeaux.application.appCommands;

// Enumération des types de commandes locales supportées.
public enum AppCommandType {
    SERVER_LIST,
    SERVER_START,
    SERVER_STOP,
    JOIN,
    PING,
    QUIT
}
